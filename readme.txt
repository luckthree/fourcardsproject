git add filenamehere.html
git add .     <-- add all modified files in directory
git commit -m "This is a message"
git push

git pull

git status

-b ( creating new branch )

u ( upstream )

git checkout master ( will change git bash local repository to master )

diff ( can show difference between two repository